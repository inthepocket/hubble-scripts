const FILE_TYPES = Object.freeze({
  SKETCH: 'SKETCH',
  FIGMA: "FIGMA"
});

const SKETCH_MODES = Object.freeze({
  DOCUMENT: 'document',
  ARTBOARD: 'artboard',
  LAYERSTYLE: 'layerstyle'
});

module.exports = {
  FILE_TYPES,
  SKETCH_MODES,
}
